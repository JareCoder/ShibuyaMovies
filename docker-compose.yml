version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "${API_PORT}:${API_PORT}"
    env_file:
      - .env
    environment:
      - DATABASE_PATH=/app/data/movies.db
    volumes:
      - ./data:/app/data
    restart: unless-stopped
